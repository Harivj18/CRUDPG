const multiplywithout = (m, n) => {
    let max = Math.max(m,n)
    let min = Math.min(m,n);
    let sum = 0;
    for (let i = 0; i < min; i++) {
        sum += max 
    }
    return sum
}

console.log('multiplywithout :', multiplywithout(3,4)); //12
